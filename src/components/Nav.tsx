import { MenuIcon } from 'lucide-react';

import { Button } from '.';

const Nav = () => {
  return (
    <>
      <Button size="icon" variant="ghost">
        <MenuIcon />
      </Button>
    </>
  );
};

export default Nav;
